import mammoth from "mammoth";

// on dev, Parcel will serve /src/index.html
window.addEventListener("DOMContentLoaded", () => {
  document.getElementById("viewer").textContent =
    "Ready to render your DOCX here.";
});
