{
  "name": "resumate",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently --names \"API,UI\" --prefix-colors \"cyan,magenta\" \"uvicorn backend.api.main:app --reload\" \"npm run dev --prefix frontend/resumate-webapp\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
